---
# Repository Configuration
repo_url: "https://github.com/weholt/test-haiku.git"
base_branch: "main"
branch: "auto/opencode-final-test"

# Create the repository if it doesn't exist
create_repo_if_missing: true

# Docker Configuration
docker_image: "repo-agent:latest"
# dockerfile: "Dockerfile"  # Uncomment to build custom image

# Authentication (choose one method)
# Option 1: Use environment variable (recommended)
github_token_env: "GITHUB_TOKEN"
# Option 2: Direct token (less secure)
# github_token: "ghp_your_token_here"
# Option 3: SSH (if you prefer SSH over HTTPS)
# use_ssh: false

# LLM Configuration
model: "anthropic/claude-3.5-haiku"
api_key_env: "OPENROUTER_API_KEY"

# Tool Configuration
tool:
  name: "opencode"
  entrypoint: "opencode run"
  args: {}

# Strategy: how the LLM approaches the task
# - "agentic": LLM plans and executes autonomously
# - "direct": LLM applies changes directly
strategy: "agentic"

# Git Configuration
git_user_name: "weholt"
git_user_email: "weholt@users.noreply.github.com"

# Commit and PR Configuration
commit_message: "Add hello world to README"
pr_title: "🎉 Add Hello World to README"
pr_body: |
  This PR adds a simple "Hello World" message to the README.md file.
  
  ## Changes
  - Created/updated README.md with Hello World message
  
  Generated automatically by repo-agent using Claude 3.5 Haiku via OpenRouter.

# Automation Flags
auto_commit: true
create_pr: true
---

# Instructions

Update the README.md file in the repository by adding "Generated by OpenCode" at the end of the file.

If the file doesn't exist yet, create it first with a simple Hello World message, then add "Generated by OpenCode" at the end.
